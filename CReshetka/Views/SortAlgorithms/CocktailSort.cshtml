@{
    ViewData["Title"] = "Шейкерная сортировка (Coctail Sort)";
}
<h1>@ViewData["Title"]</h1>

<div class="container">
    <p>
        Шейкерная (или "коктейльная") сортировка является вариацией сортировки пузырьком. Этот алгоритм сортировки также известен как "перемешивающая сортировка".
    </p>
    <p>
        Принцип работы алгоритма заключается в том, что он сначала идет слева направо, как сортировка пузырьком, перемещая наибольший элемент вправо. Затем он идет справа налево, перемещая наименьший элемент влево. Этот процесс повторяется до тех пор, пока массив не станет отсортированным.
    </p>

    <p>
        <h3><span>Пример шейкерной сортировки на C#:</span></h3>
    </p>

<pre>
<code> 
using System;

class CocktailSort
{
    static void Main()
    {
        int[] array = { 64, 34, 25, 12, 22, 11, 90 };

        Console.WriteLine("Исходный массив:");
        PrintArray(array);

        CocktailSortAlgorithm(array);

        Console.WriteLine("\nОтсортированный массив:");
        PrintArray(array);
    }

    static void CocktailSortAlgorithm(int[] arr)
    {
        bool swapped;
        do
        {
            swapped = false;

            // идем слева направо
            for (int i = 0; i < arr.Length - 1; i++)
            {
                if (arr[i] > arr[i + 1])
                {
                    Swap(ref arr[i], ref arr[i + 1]);
                    swapped = true;
                }
            }

            if (!swapped)
                break;

            swapped = false;

            // идем справа налево
            for (int i = arr.Length - 2; i >= 0; i--)
            {
                if (arr[i] > arr[i + 1])
                {
                    Swap(ref arr[i], ref arr[i + 1]);
                    swapped = true;
                }
            }
        } while (swapped);
    }

    static void Swap(ref int a, ref int b)
    {
        int temp = a;
        a = b;
        b = temp;
    }

    static void PrintArray(int[] arr)
    {
        foreach (var item in arr)
        {
            Console.Write(item + " ");
        }
        Console.WriteLine();
    }
}    
</code>
</pre>

    <p>
        <h3><span>Визуализация сортировки:</span></h3>
    </p>

    <div style="align-content:center">
        <iframe width="1280" height="720" src="https://www.youtube.com/embed/njClLBoEbfI?list=PLZh3kxyHrVp_AcOanN_jpuQbcMVdXbqei" title="Cocktail Shaker Sort" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe>
    </div>

    <p>
        <h3><span>Объяснение от <a href="https://www.youtube.com/CODEBLOG">CODE BLOG</a>:</span></h3>
    </p>
    <div style="align-content:center">
        <iframe width="1280" height="720" src="https://www.youtube.com/embed/PoWhE3N4Vr8?list=PLIIXgDT0bKw4n6pwBjymd3wCPyQ3_SLMo" title="Шейкерная сортировка (cocktail sort) - Алгоритмы C#" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe>
    </div>
</div>
